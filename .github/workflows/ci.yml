name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      S52_VERSION: "0.5.5"
      S52_RELEASE_MAVEN_ZIP: "https://github.com/bareboat-necessities/s52-kotlin-webgl/releases/download/v0.5.6/s52-kotlin-webgl-release-maven-0.5.6.zip"
      S52_RELEASE_SOURCE_ZIP: "https://github.com/bareboat-necessities/s52-kotlin-webgl/releases/download/v0.5.6/s52-kotlin-webgl-0.5.6-symbology-source.zip"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: tools/ci-render-snapshot/package-lock.json
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'
      - name: Download s52-kotlin-webgl Maven release
        shell: bash
        run: |
          mkdir -p build/s52-maven
          curl -fL "$S52_RELEASE_MAVEN_ZIP" -o /tmp/s52-kotlin-webgl-release-maven.zip
          unzip -q /tmp/s52-kotlin-webgl-release-maven.zip -d build/s52-maven
          find build/s52-maven -maxdepth 4 -type f | sort | head -40
      - name: Download s52-kotlin-webgl source release for JS composite build
        shell: bash
        run: |
          mkdir -p build/s52-source-unpacked
          curl -fL "$S52_RELEASE_SOURCE_ZIP" -o /tmp/s52-kotlin-webgl-source.zip
          unzip -q /tmp/s52-kotlin-webgl-source.zip -d build/s52-source-unpacked
          S52_SOURCE_DIR=$(find build/s52-source-unpacked -name settings.gradle.kts -printf '%h\n' | head -1)
          if [ -z "$S52_SOURCE_DIR" ]; then
            echo "Could not locate s52 source settings.gradle.kts in source release" >&2
            exit 1
          fi
          echo "S52_SOURCE_DIR=${{ github.workspace }}/$S52_SOURCE_DIR" >> "$GITHUB_ENV"
          echo "Using S52 source composite build at $S52_SOURCE_DIR"
      - name: Build S-52 Kotlin symbology
        run: gradle :demo:phase12_13_14BCheck phase16BCheck :s57-core:build :s57-s52-adapter:build :s57-render-webgl:build :demo:build -Ps52.version=$S52_VERSION -Ps52MavenRepo=${{ github.workspace }}/build/s52-maven -Ps52SourceDir="$S52_SOURCE_DIR" --stacktrace
      - name: Package NOAA Statue of Liberty browser demo
        shell: bash
        run: bash .github/scripts/package-noaa-demo-artifact.sh
      - name: Generate ENC render snapshot
        shell: bash
        run: bash .github/scripts/run-ci-enc-snapshot.sh
      - name: Upload ENC render snapshot
        uses: actions/upload-artifact@v4
        with:
          name: enc-render-snapshot
          path: |
            build/ci-enc-snapshot/render*.png
            build/ci-enc-snapshot/diagnostics*.json
            build/ci-enc-snapshot/summary.txt
          if-no-files-found: error
      - name: Upload NOAA Statue of Liberty browser demo
        uses: actions/upload-artifact@v4
        with:
          name: noaa-statue-liberty-s57-webgl-demo
          path: build/noaa-statue-liberty-demo.zip
          if-no-files-found: error
