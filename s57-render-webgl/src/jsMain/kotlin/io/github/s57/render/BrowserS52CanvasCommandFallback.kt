package io.github.s57.render

/*
 * Removed by the S-52 v0.5.4 WebGL-only demo patch.
 *
 * The browser demo must not render S-52 through Canvas2D or decoded/debug
 * geometry fallbacks.  When the S-52 WebGL backend fails, the render path now
 * reports a diagnostic and leaves the chart in a failed state instead of
 * drawing a second non-WebGL representation on the same canvas.
 */
